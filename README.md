# WinTweaks
WinTweaks is a collection of small Windows utilities that adjust and improve common system behaviors.

## Tweak Programs
- FileExplorer Tweak: enables the `FolderType` key in the registry which adds `NotSpecified` so Explorer gets a performance tweak.
